<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Home</title>
</head>

<body>
    <h1>Welcome!</h1>
    <p><strong>Access Token:</strong>
        <%= accessToken %>
    </p>
    <p><strong>Refresh Token:</strong>
        <%= refreshToken %>
    </p>
    <h2>Users List</h2>
    <ul>
        <% users.forEach((user)=> { %>
            <li>
                <%= user.username %> - <%= user.password %> - Role: <%= user.role %>
            </li>
            <% }) %>
    </ul>
    <a href="/logout">Logout</a> <!-- Thêm liên kết logout -->
</body>

</html>